---
import { Icon } from 'astro-icon/components';
import { Image } from 'astro:assets';

interface Props {
    title: string;
    description: string;
    image: string;
    tags: string[];
    curseforge: string;
    modrinth: string;
}

const { title, description, image, tags, curseforge, modrinth } = Astro.props;
---

<div class="w-[300px] p-4">
    <div class="bg-gray-100 rounded-lg">
        <Image
            class="rounded w-full object-cover object-center"
            src=`/logos/${image}`
            alt="Project logo"
            width="300"
            height="300"
        />
        <div class="py-4 px-6">
            {
                !!tags.every((tag) => tag !== '') && (
                    <div class="flex items-center space-x-2 mb-4">
                        {tags.map((tag) => (
                            <span class="bg-gray-100 text-gray-800 text-xs font-medium px-2.5 py-0.5 rounded-full dark:bg-gray-700 dark:text-gray-300">
                                {tag}
                            </span>
                        ))}
                    </div>
                )
            }

            <h2 class="text-lg text-gray-900 font-medium title-font mb-2">
                {title}
            </h2>
            <p class="leading-relaxed text-sm mb-4">
                {description}
            </p>
            <div class="flex items-center flex-wrap gap-3 mt-4">
                {
                    !!curseforge && (
                        <a
                            href={curseforge}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="px-3 py-2 text-sm font-medium text-center inline-flex items-center text-white rounded-lg bg-[#F16436] hover:bg-[#cf3e0e]"
                        >
                            <Icon
                                name="simple-icons:curseforge"
                                class="w-3.5 h-3.5 text-white me-2"
                            />
                            Curseforge
                        </a>
                    )
                }
                {
                    !!modrinth && (
                        <a
                            href={modrinth}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="px-3 py-2 text-sm font-medium text-center inline-flex items-center text-white rounded-lg bg-[#00AF5C] hover:bg-[#008345]"
                        >
                            <Icon
                                name="simple-icons:modrinth"
                                class="w-3.5 h-3.5 text-white me-2"
                            />
                            Modrinth
                        </a>
                    )
                }
            </div>
        </div>
    </div>
</div>
